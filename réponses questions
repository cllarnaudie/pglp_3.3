1)La solution ne respecte pas LSP car la classe RobotStatique hérite de la classe Robot.
RobotStatique a donc la fonction avance de la classe Robot mais cette fonction ne marche pas car elle retourne seulement 
une exception.

2)public void avancerTous(ArrayList<Robots> robots){
 int i; 
 for (i=0; i<robots.size(); i++){
 robots.get(i).avance(); 
 }
}

3)Pour que le programme respecte LSP, il faudrait que la fonction avance de la classe RobotStatique soit implémenté.
